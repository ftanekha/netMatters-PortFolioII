steps:
  - name: My portfolio secrets
    env: # Or as an environment variable
      EMAIL_VALIDATOR_REGEX: ${{ secrets.EMAIL_VALIDATOR_REGEX}}
      TELEPHONE_VALIDATOR_REGEX: ${{ secrets.TELEPHONE_VALIDATOR_REGEX}}
